# Nur für eigenständige Frontend-Entwicklung außerhalb von Docker Compose
# (docker-compose.yml setzt diese Werte bereits selbst).
# Kopieren nach .env: cp .env.example .env

VITE_OIDC_AUTH_SERVER_URL=http://localhost:8180/realms/edugate
VITE_OIDC_CLIENT_ID=edugate-frontend
VITE_CONTROL_PLANE_API_URL=http://localhost:8080/admin/api/v1
